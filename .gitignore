# ============================================================
#  Starlight Launcher .gitignore
# ============================================================

# ── 本地专用：真实密钥/端点覆盖文件（绝不要提交）──────────────
# 仓库里的 src/main/resources/assets/endpoints.json 只放占位符；
# 真实密钥写在下面这个文件里，Endpoints 会按 key 覆盖前者。
src/main/resources/assets/endpoints.local.json

# ── 构建产物 ────────────────────────────────────────────────
/target/
/build/
*.class

# ── IDE ────────────────────────────────────────────────────
/.idea/
/.vscode/
*.iml
*.iws

# ── 本地游戏目录与运行数据（非工程文件）──────────────────────
/.minecraft/
/logs/
/Starlight-Launcher/
crash-report-*/

# ── 日志与临时文件 ──────────────────────────────────────────
*.log
*.tmp
